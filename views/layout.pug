doctype html
html
  head
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    title Error
    style.
      body { font-family: sans-serif; background: #f5f5f5; color: #333; margin: 0; padding: 40px; }
      .box { background: #fff; border: 1px solid #ddd; padding: 30px 40px; max-width: 520px; margin: 0 auto; }
      h1 { font-size: 16pt; margin: 0 0 12px; }
      p { font-size: 10pt; color: #555; line-height: 1.5; margin: 0; }
  body
    div.box
      block content
